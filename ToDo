Allow teacher to delete students
change .env removing FLASK_DEBUG
Make accessible to screen readers
Error if end game when no questions are sent
Make TEST user

HEROKU:
git push heroku master
heroku run import.py
heroku pg:psql
